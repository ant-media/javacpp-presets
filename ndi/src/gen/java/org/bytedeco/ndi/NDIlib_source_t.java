// Targeted by JavaCPP version 1.5.14-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.ndi;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.javacpp.presets.javacpp.*;

import static org.bytedeco.ndi.global.ndi.*;


// This is a descriptor of a NDI source available on the network.
@Properties(inherit = org.bytedeco.ndi.presets.ndi.class)
public class NDIlib_source_t extends Pointer {
    static { Loader.load(); }
    /** Default native constructor. */
    public NDIlib_source_t() { super((Pointer)null); allocate(); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public NDIlib_source_t(long size) { super((Pointer)null); allocateArray(size); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public NDIlib_source_t(Pointer p) { super(p); }
    private native void allocate();
    private native void allocateArray(long size);
    @Override public NDIlib_source_t position(long position) {
        return (NDIlib_source_t)super.position(position);
    }
    @Override public NDIlib_source_t getPointer(long i) {
        return new NDIlib_source_t((Pointer)this).offsetAddress(i);
    }

	// A UTF8 string that provides a user readable name for this source. This can be used for serialization,
	// etc... and comprises the machine name and the source name on that machine. In the form,
	//     MACHINE_NAME (NDI_SOURCE_NAME)
	// If you specify this parameter either as NULL, or an EMPTY string then the specific IP address and port
	// number from below is used.
	public native @Cast("const char*") BytePointer p_ndi_name(); public native NDIlib_source_t p_ndi_name(BytePointer setter);

	// A UTF8 string that provides the actual network address and any parameters. This is not meant to be
	// application readable and might well change in the future. This can be NULL if you do not know it and
	// the API internally will instantiate a finder that is used to discover it even if it is not yet
	// available on the network.	// The current way of addressing the value.
		public native @Cast("const char*") BytePointer p_url_address(); public native NDIlib_source_t p_url_address(BytePointer setter);

		// We used to use an IP address before we used the more general URL notification this is now
		// depreciated but maintained for compatibility.
		public native @Cast("const char*") BytePointer p_ip_address(); public native NDIlib_source_t p_ip_address(BytePointer setter);

// #if NDILIB_CPP_DEFAULT_CONSTRUCTORS
// #endif // NDILIB_CPP_DEFAULT_CONSTRUCTORS
}
